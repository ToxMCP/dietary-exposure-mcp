# Extension Packs

Optional additive packs can be placed under `defaults/extensions/v1/`.

Supported categories:

- `commodity_taxonomy/`
- `consumption_profiles/`
- `reporting_profiles/`

Each pack must be JSON and follow the conventions documented in [extension hooks](../../docs/extension_hooks.md).
